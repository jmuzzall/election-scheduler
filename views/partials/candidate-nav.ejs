<nav class="navbar navbar-expand-lg navbar-dark bg-success">
  <div class="container">
    <a class="navbar-brand" href="/candidate/portal">
      <i class="bi bi-calendar-check"></i> Sunday Duty Scheduler
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#candNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="candNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link <%= typeof activePage !== 'undefined' && activePage === 'portal' ? 'active fw-bold' : '' %>"
            href="/candidate/portal">
            <i class="bi bi-calendar-x"></i> My Availability
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= typeof activePage !== 'undefined' && activePage === 'schedule' ? 'active fw-bold' : '' %>"
            href="/candidate/schedule">
            <i class="bi bi-calendar-check"></i> View Schedule
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= typeof activePage !== 'undefined' && activePage === 'swaps' ? 'active fw-bold' : '' %>"
            href="/candidate/swaps">
            <i class="bi bi-arrow-left-right"></i> Swap Requests
            <% if (typeof pendingSwapCount !== 'undefined' && pendingSwapCount > 0) { %>
              <span class="badge bg-warning text-dark ms-1"><%= pendingSwapCount %></span>
            <% } %>
          </a>
        </li>
      </ul>
      <ul class="navbar-nav">
        <li class="nav-item">
          <span class="nav-link text-white"><i class="bi bi-person"></i> <%= candidate.name %></span>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/candidate/logout">
            <i class="bi bi-box-arrow-right"></i> Logout
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>
